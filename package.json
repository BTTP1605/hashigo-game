{
  "name": "yakobu-no-hashigo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run encrypt && vite",
    "build": "npm run encrypt && tsc --noEmit && vite build",
    "preview": "vite preview",
    "encrypt": "node scripts/encrypt-scenario.mjs",
    "genimg": "node scripts/generate-images.mjs",
    "gense": "node scripts/generate-se.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "pngjs": "^7.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
